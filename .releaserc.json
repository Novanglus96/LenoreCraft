{
  "branches": [
    {"name": "main"},
    {"name": "alpha", "channel": "alpha", "prerelease": "alpha"},
    {"name": "beta", "channel": "beta", "prerelease": "beta"},
    {"name": "rc", "channel": "rc", "prerelease": "rc"}
  ],
  "tagFormat": "v${version}",
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md"
      }
    ],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "chmod +x scripts/update-version.sh && ./scripts/update-version.sh ${nextRelease.version}"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": [
          "CHANGELOG.md",
          "scripts/version.txt",
          "backend/Dockerfile",
          "frontend/Dockerfile",
          "backend/administration/fixtures/version.json",
          "backend/backend/urls.py",
          "frontend/package.json",
          "frontend/src/views/AppNavigationVue.vue",
          "frontend/src/App.vue"
        ],
        "message": "chore(release): ${nextRelease.version} [skip ci]"
      }
    ],
    "@saithodev/semantic-release-gitea"
  ]
}
